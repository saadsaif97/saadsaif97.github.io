---
const phoneNumber = "+923314002075"; // your WhatsApp number with country code
const message = "Hi Saad I’m interested in your Shopify mentorship and courses!";
const whatsappLink = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`;
---

<style>
.whatsapp-chat {
  position: fixed;
  bottom: 20px;
  right: 20px;
  z-index: 9999;
}

.whatsapp-chat a {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 60px;
  height: 60px;
  background-color: #25D366;
  border-radius: 50%;
  box-shadow: 0 4px 6px rgba(0,0,0,0.2);
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}

.whatsapp-chat a:hover {
  transform: scale(1.1);
  box-shadow: 0 6px 10px rgba(0,0,0,0.25);
}

.whatsapp-chat img {
  width: 35px;
  height: 35px;
}
</style>

<div class="whatsapp-chat">
  <a href={whatsappLink} target="_blank" aria-label="Chat on WhatsApp">
    <img src="https://cdn.jsdelivr.net/gh/simple-icons/simple-icons/icons/whatsapp.svg" alt="WhatsApp" />
  </a>
</div>
