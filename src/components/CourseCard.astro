---
const { title, language, price, description, enrollUrl } = Astro.props;
---

<li class="bg-muted/50 overflow-hidden shadow-sm hover:shadow-md transition-shadow duration-200">
  <div class="p-4">
    <h3 class="text-lg font-semibold">{title}</h3>
    <p class="text-sm text-gray-500 mb-2">Language: <strong>{language}</strong></p>
    <p class="text-sm text-gray-400 mb-3">{description}</p>
    <div class="flex items-center justify-between">
      <span class="text-accent font-bold text-lg">${price}</span>
      <a
        href={enrollUrl}
        class="inline-block px-4 py-2 bg-accent text-white text-sm rounded hover:bg-accent/90 transition"
      >
        See more details
      </a>
    </div>
  </div>
</li>
