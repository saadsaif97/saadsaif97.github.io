---
const { title, language, price, description, enrollUrl } = Astro.props;
---

<li>
  <a
    href={enrollUrl}
    class="block overflow-hidden bg-muted/50 shadow-sm transition-shadow duration-200 hover:shadow-md"
  >
    <div class="p-4">
      <h3 class="text-lg font-semibold">{title}</h3>
      <p class="mb-2 text-sm text-gray-500">
        Language: <strong>{language}</strong>
      </p>
      <p class="mb-3 text-sm text-gray-400">{description}</p>
      <div class="flex items-center justify-between">
        <span class="text-lg font-bold text-accent">${price}</span>
        <span class="inline-block rounded border border-muted px-4 py-2 text-sm text-white transition">
          See more details
        </span>
      </div>
    </div>
  </a>
</li>
